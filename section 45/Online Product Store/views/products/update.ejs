<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Product</title>
</head>
<body>
    <h1>Update Product</h1>
    <form method="post" action="/products/<%= product._id %>?_method=PUT">
        <label for="name">Name: </label><input name="name" id="name" type="text" placeholder="product name" value="<%= product.name %>"><br>
        <label for="price">Price: </label><input name="price" id="price" type="number" step="0.01" placeholder="product price" value="<%= product.price %>"><br>
        <label for="category">Category: </label>
        <select name="category" id="category">
            <% for (let category of categories) { %>
                <option value=<%= category %> <%= category === product.category ? 'selected' : '' %>><%= category %></option>
            <% } %>
        </select>
        <br>
        <button>Submit</button>
    </form>
    <a href="/products/<%= product._id %>">Back</a>
</body>
</html>